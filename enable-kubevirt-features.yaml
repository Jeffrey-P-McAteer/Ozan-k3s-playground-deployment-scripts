apiVersion: kubevirt.io/v1
kind: KubeVirt
metadata:
  name: kubevirt
  namespace: kubevirt
spec:
  configuration:
    developerConfiguration:
      featureGates:
        - HostDisk
# Alternatively, run
#   kubectl edit kubevirt kubevirt -n kubevirt


